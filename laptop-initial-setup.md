cp flash/backup ~/
(.ssh, .claude, bashrc and gitignore)

sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt-get update
sudo apt install -y git curl unzip redis
setup rus keyboard layout

install chrome-stable
install lastpass extension
login esemiko@gmail.com by lastpass
enable chrome sync
(ublock origin, youtube enchancer, pins and bookmarks)

install claude + personal subscription

install telegram + login
setup telegram autorun

install pycharm-community + import settings
install idea-community
setup icons for IDE-s

chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_rsa
chmod 644 ~/.ssh/*.pub

mkdir -p ~/development && cd  ~/development
git checkout esemi.github.io
git checkout invest_stat from github
git checkout official-documents from github

sudo apt-get install sbt
sudo apt-get install gnupg curl
sudo apt-get install -y default-libmysqlclient-dev pkg-config gcc gettext-base


>>>>>>>>>>>>>>>>>>>>>>>>> todo next
setup marblemouse


скопировать все репы с прошлоё тачки
скопировать фотки с внешнего диска